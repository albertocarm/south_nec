% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolkit.R
\name{cure_model}
\alias{cure_model}
\title{Define a Promotion Time Cure Model specification}
\usage{
cure_model(
  dat,
  clonogenic,
  kinetic,
  prior_clonogenic = 1,
  prior_kinetic = 1,
  prior_intercept = 2.5,
  stan_file = stan_model_file(),
  spline_vars = NULL
)
}
\arguments{
\item{dat}{A data.frame (already pre-processed, see [prep_data()]) with
`EFS_time` and `.status_bin`.}

\item{clonogenic}{A one-sided formula for the incidence (log-theta) linear
predictor, e.g. `~ A * B + C`.}

\item{kinetic}{A one-sided formula for the latency (log-lambda) linear
predictor.}

\item{prior_clonogenic}{Scalar, unnamed vector, or named list of prior SDs
for the clonogenic coefficients (see details).}

\item{prior_kinetic}{Same structure as `prior_clonogenic`, for latency.}

\item{prior_intercept}{Scalar prior SD for both intercepts.}

\item{stan_file}{Path to the `.stan` file. Defaults to the bundled model.}

\item{spline_vars}{Optional named list of pre-computed spline bases.}
}
\value{
An object of class `cure_spec`.
}
\description{
Define a Promotion Time Cure Model specification
}
\details{
`prior_*` accepts:
* a single scalar (same SD for every coefficient),
* a numeric vector of length K (positional),
* a named list: every name is grep-matched against the `model.matrix`
  columns; matching columns receive the supplied SD, the rest default
  to 1.
}
