% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interaction_screen.R
\name{screen_interactions}
\alias{screen_interactions}
\title{Systematically screen pairwise interactions in the clonogenic submodel}
\usage{
screen_interactions(
  data,
  base_clonogenic,
  kinetic,
  pairs            = NULL,
  labels           = NULL,
  prior_clonogenic = NULL,
  prior_kinetic    = NULL,
  prior_intercept  = 2.5,
  chains           = 4,
  iter             = 4000,
  warmup           = floor(iter/2),
  seed             = 42,
  adapt_delta      = 0.95,
  max_treedepth    = 12,
  prob             = 0.95,
  meaningful_se    = 2,
  verbose          = TRUE,
  ...
)
}
\arguments{
\item{data}{A `data.frame` already pre-processed by \code{\link{prep_data}}.}

\item{base_clonogenic}{One-sided formula for the additive clonogenic linear
predictor.}

\item{kinetic}{One-sided formula for the latency linear predictor.}

\item{pairs}{List of length-2 character vectors naming the variables to
interact one at a time. Defaults to all unordered pairs of the variables
appearing in \code{base_clonogenic}.}

\item{labels}{Optional named character vector mapping \code{"a:b"} term
names to display labels. Both orderings are recognised.}

\item{prior_clonogenic, prior_kinetic, prior_intercept}{Forwarded to
\code{\link{fit_cure_bayes}}.}

\item{chains, iter, warmup, seed, adapt_delta, max_treedepth}{Forwarded to
\code{\link{fit_model}} via \code{\link{fit_cure_bayes}}.}

\item{prob}{Credible interval width. Default \code{0.95}.}

\item{meaningful_se}{Multiplier of \code{se_diff} above which an ELPD
difference is flagged as meaningful. Default \code{2}.}

\item{verbose}{If \code{TRUE}, prints progress for every fitted model.}

\item{...}{Additional arguments forwarded to \code{\link{fit_cure_bayes}}.}
}
\value{
A \code{data.frame} of class
\code{c("interaction_screen", "data.frame")} sorted by strength of
evidence, with one row per supplied pair. Carries
\code{attr(., "loo_base")}, \code{attr(., "fit_base")} and
\code{attr(., "fits_int")} so the underlying objects can be inspected.
}
\description{
Fits the additive base model and one model per supplied pair (base + single
interaction term). For every interaction model, the function reports the
posterior median and 95\% credible interval of the interaction coefficient
on the original (un-standardised) covariate scale, the probability of
direction, and the LOO-CV ELPD difference relative to the additive base
model. Results are sorted by strength of evidence so the table can be
rendered directly as Supplementary Table A5.
}
\seealso{
\code{\link{format_interaction_table}}, \code{\link{fit_cure_bayes}},
\code{\link{loo_fit}}.
}
